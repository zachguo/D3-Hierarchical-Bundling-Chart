# d3-hierarchical-bundling-chart
A reusable &amp; responsive hierarchical bundling chart based on D3.js for flat JSON data

- CSS injection and configurable user styling
- working for csv-like flat JSON data
- tooltip
